"use client";

/**
 * OpenRecentDialog component has been removed.
 *
 * Recent databases are now handled via the CustomTitleBar submenu.
 * This file is intentionally left as a stub to document the removal
 * and to avoid re-introducing an unused dialog component.
 */
